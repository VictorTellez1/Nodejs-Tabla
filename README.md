# Notas:
```
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base de la tabla de multiplicar       [number] [required]
  -l, --listar   Muesta la tabla en consola[boolean] [required] [default: false]
  -h, --hasta    Esta es el numero hasta el que va a llegar la tabla
                                                           [number] [default: 1]
```